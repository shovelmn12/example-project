import { type Service, type ServiceID } from "..";

export const defaultService: Service = {
  id: "srv_default" as ServiceID,
  name: "Default Service",
  description: "This is a default service.",
};
